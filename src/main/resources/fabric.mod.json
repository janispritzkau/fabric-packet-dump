{
  "schemaVersion": 1,
  "id": "packet-dump",
  "version": "${version}",
  "name": "Packet Dump",
  "environment": "*",
  "entrypoints": {
    "main": []
  },
  "mixins": [
    "packet-dump.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${loader_version}",
    "minecraft": "${minecraft_version}"
  }
}
